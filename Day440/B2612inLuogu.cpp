/*****************************************************************
 * 题目：【深基1.习4】简单的分苹果 3
 * 描述：根据分苹果的剩余，求解苹果总数。
 * 思路：联立方程组，见B2612 Solve。
 * 空间：Tools模拟工具包。
 * 注意：输出格式。
 ****************************************************************/

#include <bits/stdc++.h>

using namespace std;

namespace Tools
{
#define rep(i, s, k) for (int i = s; i <= k; i++) // 定义快速循环
#define ll long long                              // 定义长整型
#define ull unsigned long long                    // 定义无符号长整型
#define div_up(a, b) (a) / (b) + ((a) % (b) != 0) // 定义 上整除
};

using namespace Tools;

int main()
{
    cout << 12 << " " << 47 << endl;
    return 0;
}